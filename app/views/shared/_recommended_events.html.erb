<% if @recommended_events.any? %>

  <h3>Recommended events for you</h3>
  <br>
  <div class="row">
    <div class="col-xs-12 col-sm-offset-2 col-sm-8">
      <ul class="event-list">
        <%= render partial: 'events/event', collection: @recommended_events %>
      </ul>
    </div>
  </div>

<% end %>